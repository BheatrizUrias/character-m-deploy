// abilities_editor.js - Versão inicial apenas para carregamento
export function populateEquipmentSection(currentCharacter) {
  // Por enquanto não faz nada, apenas permite o carregamento do partial
  console.log('Seção de Atributos carregada');
  
  // Você pode adicionar aqui qualquer inicialização básica necessária
  // Mas conforme solicitado, não vamos implementar a lógica ainda
}